name: case_study
version: "1.0"
config-version: 2

profile: citibike_snowflake
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  citibike:
    raw:
      +schema: raw # models/staging → schema = raw

    refined:
      +schema: refined # models/marts → schema = refined
